"""PyQt5 GUI for MedSAM2 mammography annotation.

The reusable inference stack lives under ``medsam2_experiments/interactive/``.
This package only provides the GUI shell (canvas, panels, threading, theming,
session memory, YOLO export) on top of that stack.
"""
